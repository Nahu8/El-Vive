{
  "name": "el-vive",
  "private": true,
  "description": "Monorepo: frontend/ (Angular) + backend/ (Express). Hostinger: directorio root del repo = ./ (vacío o punto); archivo de entrada backend/src/index.js; build: npm run build; start: npm start.",
  "engines": {
    "node": ">=18 <=24"
  },
  "scripts": {
    "start": "node backend/src/index.js",
    "start:backend": "npm run start --prefix backend",
    "build": "npm install --prefix frontend --include=dev && npm install --prefix backend && npm run build --prefix backend"
  }
}
