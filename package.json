{
  "name": "el-vive",
  "private": true,
  "description": "Monorepo Angular+Express. Hostinger: root=./, build=npm run build, Start command=npm start (obligatorio si no arranca), entry=backend/src/index.js. Tras deploy revisar backend/startup-debug.log en el servidor.",
  "engines": {
    "node": ">=18 <=24"
  },
  "scripts": {
    "start": "node backend/src/index.js",
    "start:backend": "npm run start --prefix backend",
    "build": "npm install --prefix frontend --include=dev && npm install --prefix backend && npm run build --prefix backend"
  }
}
