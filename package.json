{
  "name": "el-vive",
  "private": true,
  "description": "Monorepo Angular+Express. Hostinger: root=./, build=npm run build, start=npm start, entry=server.js. Si falla el runtime, revisar stderr.log o startup-debug.log.",
  "engines": {
    "node": ">=18 <=24"
  },
  "scripts": {
    "start": "node server.js",
    "start:backend": "npm run start --prefix backend",
    "build": "npm install --prefix frontend --include=dev && npm install --prefix backend && npm run build --prefix backend"
  }
}
